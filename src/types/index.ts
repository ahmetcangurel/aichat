export type MessageItemTypes = {
  id: number;
  message: string;
  isUser: boolean;
  time: string;
  date: string;
};

export type UserTypes = {
  bio: string;
  createdAt: string;
  email: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  photoUrl: string;
  regionCode: string;
  uid: string;
  updatedAt: string;
  password: string;
};
